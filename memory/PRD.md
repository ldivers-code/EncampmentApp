# CAP Encampment App — PRD & Changelog

## Original Problem Statement
Build an interactive roster & management application for the **Tennessee Wing
Civil Air Patrol Encampment** with:
- Roster Management (live filtering, RBAC, flight-grouped views, contact info)
- Student Upload (Excel) with auto-flight assignment + sub-event handling
- Org Chart (strict 1:1 interactive mind-map)
- Health Services (allergies, OTC approvals, Daily Med Diary, Supplements, Contraband)
- Check-In & Barracks (multi-step in-processing)
- Granular RBAC with visibility / redaction / cascading assignments
- Parent Portal "My Cadet" + Admin widget editor
- Budget/Finance Tracker + Smart Receipt OCR
- Schedule Sync (Excel auto-publish)
- Honor Agreements (digital signature)
- Mobile App back-end ready

## Stack
- React 19, Tailwind, Shadcn UI, DOMPurify, lucide-react
- FastAPI, MongoDB, Python
- emergentintegrations (GPT-4o Vision OCR); SendGrid email
- JWT auth (httpOnly cookie + Bearer header); bcrypt

## Architecture (current)
```
/app/
├── backend/
│   ├── models.py              # Roles, Permissions, ParticipantTypes
│   ├── role_groups.py         # 7-concern separation, semantic groups (Phase 3)
│   ├── scope.py               # Visibility filtering & Redaction rules
│   ├── classifier.py          # Unified RegZone sub-event classifier
│   ├── permissions.py         # JWT, get_current_user, require_role, require_health_*
│   ├── database.py            # Centralized DB logic & participant counts
│   └── routes/                # auth, users, participants, …
└── frontend/src/{pages,components}
```

## Seven Separated Concerns (Phase 3)
1. **Account status** — `users.is_approved`; surfaced by `GET /api/auth/status`
2. **CAP member type** — `participants.member_type` (SENIOR / CADET / CADET SPONSOR)
3. **Encampment participant type** — `participants.participant_type` (`student | cadre | senior_staff | needs_review`)
4. **Cadre role type** — `participants.is_exec_cadre` + `users.cadre_position`
5. **Duty assignment** — `users.flight / squadron / cadre_unit / cadre_position / support_section`
6. **Permission role** — `users.role` (UserRole) grouped via `role_groups.py`
7. **Access scope** — `scope.py` (visibility filter + field redaction)

## Implementation Log
- **Phase 1**: Backend classification audit (canonical taxonomy proposal).
- **Phase 2** (Feb 2026): DB migration — all participants normalized to `student / cadre / senior_staff / needs_review`; added `is_exec_cadre` flag; centralized `get_active_participant_count`; created `scope.py` for visibility/redaction.
- **Phase 3** (Feb 2026): Separated the seven concerns; introduced `role_groups.py`; tightened EXEC_CADRE so it cannot leak into senior-staff capabilities by default; added `cadre_lead_can_target()` guard. 22 unit tests + 9 live RBAC tests passing.
- **Phase 4** (Feb 2026): Approval & participant linking hardened. New `account_status.py` (single source of truth for the six canonical fields). New `get_current_user_linked` dependency. Approve endpoint now tolerates id/email/capid identifiers, returns `matched_by`, structured 404 with `tried` array. Pending-users list now embeds the canonical status block. New `GET /api/users/{id}/status` (admin). 8 new unit tests passing.
- **Phase 5** (Feb 2026): Sensitive data & exports consolidation. Extended `scope.py` with `safe_roster_entry`, `redact_payment_only`, `visible_flights_for`, `can_view_payment`, `can_view_medical`. Fixed three concrete leaks: `/participants/pending-payments` (now finance/admin-only + scoped + redacted), `/flights/{flight}/roster` & `/squadrons/{squadron}/roster` (now visibility-scoped 403 + redacted via safe_roster_entry), summary-export "Pending Payments" sheet (only emitted for finance callers). Stats endpoint updated to canonical taxonomy. 29 new scope tests passing.
- **Phase 6** (Feb 2026): Participant-count consistency. Every surface now sources its encampment-wide count from `get_active_participant_count()`. Added `total_active` to `/participants/stats` and `/participants/analytics/detailed` (so cadre/scoped callers see both their visible count AND the canonical encampment total). Added new `/api/barracks/summary` endpoint. Replaced `len(participants)` total in `/participants/payment-summary` with the helper. Live verification: 9 surfaces, identical count.
- **Schedule Grid Parser** (Feb 2026): `sync_schedule_from_gsheet` now auto-detects and parses the per-squadron grid layout used by the CAST and Encampment day-tab sheets in addition to the flat-table format. Detection: header row with `START / END / <squadron columns> / Notes`. Date extracted from cell A1 (e.g. "Friday | Day 1 CADRE Arrival | May 29th"); year falls back to `LAST UPDATED:` footer then current year. Merged horizontal cells (only first squadron column filled) are emitted as one event targeting all squadrons in the block; per-squadron filled cells emit per-squadron events. Vertically-merged cells across time slots are stitched back into one longer event by extending `end_time`. Notes column is treated as row-wide and copied into every emitted event. 12 unit tests in `test_schedule_grid_parser.py`. Verified live against the CAST Day 1 sheet — 9 correctly-merged events synced.
- **Frontend Taxonomy Sweep** (Feb 2026): Replaced remaining legacy participant_type literals in `HealthServicesDashboard.js`, `BarracksPage.js`, `AdminPage.js` (Annual Reset options), `FlightManager.js`, `PaymentReportsTab.js`, and `AnalyticsPage.js`. The four canonical values (`student`, `cadre`, `senior_staff`, `needs_review`) are now used consistently across UI filters, drag-and-drop chips, dashboards, and bulk-reset selectors. `is_exec_cadre` is treated as a boolean flag rather than a participant_type. `RosterPage.js` keeps the legacy aliases ONLY as defensive read-time fallbacks (so un-migrated DB rows still render). Lint clean.
- **Admin Schedule Settings UI** (Feb 2026): Replaced the "grid not supported" warning with a structured two-format hint card explaining both flat-table and grid layouts, including the cell A1 date convention and merge-behaviour.
- **Mobile Backend Integration Doc** (Feb 2026): Rewrote `§3 User Roles & Permissions` to document the seven-concern model from `role_groups.py`, the canonical four-value `participant_type`, the six fields returned by `/api/auth/status`, and per-status mobile UX guidance. Updated the `Participant` data shape to reflect the new taxonomy and the new `is_exec_cadre` boolean.
- **Schedule Tab Auto-Discovery** (Feb 2026): One-paste workflow for bulk-importing every day-tab from a Google Sheets spreadsheet. New endpoints `POST /api/google-sheets/discover-tabs` (downloads XLSX export via openpyxl, lists every tab with A1 title + parsed schedule date) and `POST /api/google-sheets/schedules/bulk-add` (appends to `settings.schedules`, deduped by `(spreadsheet_id, sheet_name)`, auto-suffixes colliding ids). `ScheduleSheetConfig` gained an optional `sheet_name` field; `fetch_google_sheet_csv` now resolves tabs as `gid → sheet_name (via gviz/tq) → first tab`. Admin UI: new "Discover Tabs" button + modal listing each tab with parsed-date badge, A1 preview, editable label, and a checkbox pre-checked for schedule-like tabs. Verified live against the CAST sheet (4 tabs found, 3 imported, 54 events synced from "Sat May 30th"). 7 new backend tests + 12 regression tests pass.
- **Auto-Balance Fix + Waitlist by Application Order** (Feb 2026): `auto_assign_flights`, `auto_assign_single_student`, and `auto_assign_unassigned_students` were silently no-op'ing because they filtered by the deprecated `participant_type ∈ {basic_student, advanced_student}`. Replaced with canonical `student` everywhere; ditto the `total_students` count and `flight_distribution` loop in the upload endpoint. Added `AppEditData → app_edit_data` to both the Excel upload and Google Sheets roster column maps; the assigner now sorts `students_to_assign` by `app_edit_data` ascending so the **earliest applications get the seats** and late applicants stay on the **waitlist**. Each flight is enforced at 3 elements × 5 cadets = 15 (6 × 15 = 90 total). When Sync Mode soft-removes a student from the roster, an auto-promotion pass immediately fills the open seat from the highest-priority waitlisted cadet. Auto-assign also normalises mixed-case flight values (`Alpha` → `alpha`) on every run to keep flight-grouped counts consistent. Frontend toast now reports both `assigned` and `waitlisted` counts. 6 new unit tests + existing student-upload regression all pass. Verified live: 101 students → 90 seated, 11 on waitlist; dropping one student → 1 promoted from waitlist.
- **Waitlist UI** (Feb 2026): Added `app_edit_data` to the `ParticipantResponse` model so the field reaches the frontend. Added a one-click **"Waitlist: N"** amber chip next to the participant count header on `RosterPage.js` that toggles the flight filter to `unassigned` and auto-switches to "All" roster view (so waitlisted students — who have no flight — actually appear). Added an info banner above the table explaining the cap (3×5=15) and how to manually promote (click any flight cell — uses existing inline-edit, no new endpoint needed). The flight cell on waitlisted rows now shows a yellow `WAITLIST` badge with the application date below it. When the waitlist filter is active, rows sort by `app_edit_data` ascending so the longest-waiting cadet is first.
- **Mobile Backend Integration — Recent Changes Section** (Feb 2026): Added new `§1a. Recent Backend Changes (Mobile-Relevant)` to `MOBILE_BACKEND_INTEGRATION.md` covering: (1) canonical `participant_type` deprecation guidance with the four allowed values + `is_exec_cadre` boolean, (2) waitlist & application-order auto-assign rules including `app_edit_data`, the 3×5=15 cap, the new `promoted_from_waitlist` field in upload responses, and mobile UI guidance for waitlist rendering, (3) schedule grid-format parser semantics + the `target_groups` filtering pattern + the new `discover-tabs` and `bulk-add` admin endpoints, (4) finance-tracker sync + `/api/budget/finance/needs-review` endpoint, (5) Participant data-shape additions (`app_edit_data`, `is_exec_cadre`, numeric `amount_paid`, `is_removed`). Also updated the Participant/Schedule Event data shape examples and the Roster/Google Sheets endpoint tables with new endpoints + response shapes.
- **Mobile Integration Doc as API** (Feb 2026): New `routes/mobile.py` exposes the markdown spec as three endpoints — `GET /api/mobile/integration-doc/version` (cheap polling — returns `{version, sha256, last_modified, doc_url}`), `GET /api/mobile/integration-doc` (full JSON-wrapped body with metadata), `GET /api/mobile/integration-doc?format=markdown` (raw markdown with `X-Doc-Version`/`X-Doc-SHA256` response headers, suitable for CI `curl > spec.md`). All endpoints are public (the doc describes only the public API surface). Added a CI usage snippet to the doc itself so mobile devs discover the live endpoint on first read. 4 tests pass.
- **Support Cadre / Senior Member Taxonomy** (Feb 2026): Fixed the bug where support cadre were showing as "Waitlist" because their `flight` field was empty by design. Added three new UserRole values (`superintendent`, `chief_training_officer`, `public_affairs`) and a new `support_sections.py` module that centralizes the seven-concern taxonomy: `SENIOR_MEMBER_ROLES` (commander, dcp, executive_staff, training_officer, superintendent, chief_training_officer), `SUPPORT_CADRE_ROLES` (the 5 `support_*`), `SUPPORT_SENIOR_STAFF_ROLES` (logistics, finance, plans_programs, health_services, dining_facility, public_affairs, staff). 8 canonical support sections: Logistics, Communications, Public Affairs, Dining, Health, Plans/Programs, Training, Finance. New squadron buckets: `support_cadre`, `support_senior_staff`, `senior_member`. New endpoint `GET /api/taxonomy/support` exposes the full taxonomy to the frontend. The `/api/participants` listing now enriches each row with `linked_user_role`, `linked_support_section`, `is_non_flight`, `is_support` (batched single users.find). The `PUT /api/participants/{id}/assignment` endpoint now accepts support-section slugs as `flight` and, when changed, **syncs back to the linked user** by updating `user.role` (e.g. `support_logistics` → `support_comms` when section changes from logistics to communications) AND `user.support_section`. Frontend (`RosterPage.js`): the Flight cell now renders a dedicated section dropdown (8 options) for `is_non_flight` participants, an indigo section badge instead of a yellow Waitlist badge; the Squadron cell offers `Support — Cadre / Support — Senior Staff / Senior Member` for the same group; the waitlist count excludes `is_non_flight` participants. Verified live: Test Cadre (role=support_logistics) now displays `SUPPORT — CADRE` squadron + `— set section —` flight prompt instead of "Waitlist", and changing the section via the dropdown propagates to the linked user account. Also fixed the `AppEditData` → `AppEditDate` column-name typo (parser now accepts both). 10 new unit tests + roster-user sync integration test all pass.
- **Roster Assignment Hardening + Exec Cadre Role Mgmt** (Feb 2026): Applied a user-supplied patch bundle that enforces the 15-student cap on EVERY write path (auto-assign, drag/drop, inline edit, bulk-assignment, Excel upload), introduces an idempotent re-upload (no duplicates), and converts the legacy "soft-remove app-only rows" Sync Mode into a "review-flag" workflow. New helpers in `routes/students.py`: `STUDENT_PARTICIPANT_TYPES` (canonical + legacy aliases), `is_student_participant_type`, `normalize_flight`, `get_student_count_for_flight`, `assert_student_flight_capacity` (returns HTTP 409). `auto_assign_flights` gained a `reassign_existing` flag — when true (used by every spreadsheet upload) it rebuilds seating from scratch using `AppEditDate` ascending, so historical over-cap flights self-correct. Blank `SubEvents` → `participant_type='needs_review'` + flight=None + review_reason set. App-only rows missing from a new upload now get `review_status='needs_review'` instead of being soft-removed; the auto-promotion-on-soft-remove block was removed (promotion happens naturally on the next source upload). `routes/participants.py` `PUT /assignment` and `PUT /bulk-assignment` both call `assert_student_flight_capacity` for student moves (cadre/senior-staff exempt). Added 4 review fields to `ParticipantBase`: `review_status`, `review_reason`, `review_flagged_at`, `review_flagged_by`. **Item #10 — Exec Cadre role management**: rewrote `PUT /api/users/{user_id}/role` to (a) accept Exec Cadre actor, (b) scope them to the cadre-bucket roles only (`EXEC_CADRE_MANAGEABLE_ROLES`), (c) block them from editing any user whose current role is not cadre-bucket, (d) block them from granting protected/system roles (commander, dcp, executive_staff, superintendent, chief_training_officer), (e) write every change to a new `role_audit_log` collection with actor/target/previous/new/timestamp. New endpoint `GET /api/users/role-audit-log` (Exec Cadre sees only their own entries; full admins see all). Created `exec_cadre@test.com` / `test123` for acceptance tests. **8 new acceptance tests in `test_roster_assignment_fix.py` + 41 existing regression tests all pass.**
- **Org Chart Canonical Template & Live Sync** (Feb 2026): Phase 1+2 of the 17-point Org Chart overhaul. Backend now drives `db.org_chart_roles` from `org_chart_template.POSITION_TEMPLATE` (79 canonical positions). New endpoints: `GET /api/org-chart/template` (read-only template + categories), `POST /api/org-chart/seed?reset=bool` (defaults to **preserve mode** — reconciles template metadata, retains existing `assigned_name`, prunes orphans, returns `{inserted, updated, deleted, preserved_assignments}`), `POST /api/org-chart/seed-defaults` (destructive reset), `POST /api/org-chart/resync-from-users` (rebuilds every assignment from current users collection). Refactored `auto_sync_org_chart` via new `org_chart_template.find_role_id_for_user` helper with explicit precedence: **role-as-position** (squadron_commander, training_officer, senior-staff, support_*) > **cadre_position + flight/squadron** > **cadre_unit hint**. Now also clears the previously occupied position before writing the new one (`assigned_user_id` tracking) so a person never appears in two slots. Live sync now fires from BOTH `PUT /api/users/{user_id}/role` AND `PUT /api/users/{user_id}/unit` AND `POST /users/{user_id}/link-participant`. Public Affairs canonically moved under DCS (`public-affairs-dept`); squadron enlisted leads are now "First Sergeant" not "Superintendent"; SM Superintendent slot added at top level. 17 new acceptance tests in `test_orgchart_phase1_2.py` all pass; legacy `test_orgchart{,_v2,_v3}.py` deprecated via module-level skip (they asserted obsolete 85/86-position counts).
- **Org Chart UI — Filtered Branch Views + Strict Colors** (Feb 2026): `OrgChartPage.js` layout engine now respects the active category filter. When a branch is selected (6th CTS / 21st / 22nd / Cadet Support / Adult Support / Staff), the page computes the visible subtree = filtered nodes + every ancestor up to root, builds a **filtered child-map** so unrelated subtrees occupy zero canvas width, auto-expands collapsed nodes within the visible set, and auto-recenters the scrollport. Result: selecting "6th CTS" now shows ONLY the 6th CTS chain (Encampment Commander → Commandant → CTG/CC → 6th CTS Commander → Squadron TO + First Sergeant + Flight A/B Commanders/Sergeants) with no empty canvas. Added new `support` category to the color palette (Emerald — Adult Support / DCS branch). Strict palette verified end-to-end: Blue 6th CTS, Yellow 21st, Maroon 22nd, Emerald Staff + Adult Support, Silver Cadet Support. The "Reseed" button now triggers preserve-mode `/seed` and its confirm copy reflects the non-destructive behaviour.
- **RichTextEditor XSS Hardening** (Feb 2026): `RichTextDisplay` now passes a strict `ALLOWED_TAGS` (p/br/strong/em/u/ol/ul/li/s) + empty `ALLOWED_ATTR` config to DOMPurify, narrowing the allowlist to exactly what Quill emits. Added an `escapeHtml` pass inside `plainTextToHtml` so legacy plain-text bullets can't smuggle raw markup into the HTML output before sanitization.
- **Inspections & Points (replaces legacy Point Tracking)** (Feb 2026): Wholesale replacement of `routes/points.py` + `PointsPage.js` with a new module rebuilt from the Google Sheet at `1el_Xsuv1PnmOqFkzSVLWag_KSTICR3wmyTf0v4Ei11E`. Backend `routes/inspections.py` exposes `/api/inspections/{types,settings,scores,merit-points,dashboard,student/{pid}}` with strict RBAC (`exec_cadre / executive_staff / plans_programs / commander / dcp`). Five inspection types — `dorm_uniform` (Day-2 #1 initial, weight=20), `dorm_uniform_repeat` (Day-2 #2/4/6, weight=100), `drill` (18 movements /(18*3)), `daily_sports` (/20), `knowledge` (9 Q's /9). Per-cadet rows store `absent: bool` so blanks never count as 0. Aggregation server-side recreates the workbook formulas: CTF Avg = AVG of inspection % that ran, CTF Pts = SUM(% × weight), CTS Avg/Pts = AVG/SUM of squadron's two flights, Weekly Totals = SUM CTF Pts per flight, Weekly Change = Day N − Day N−1 (Day 1=0). Settings expose the 5 weights (TOTALS U22:W26) + Day→Inspections map + Merit-Points toggle. Frontend `InspectionsPage.js` with 5 sub-tabs (Dashboard, Enter Scores, Weekly Totals, Student Points, Settings); mobile-friendly tables; row-level Absent checkbox. Deleted: `routes/points.py`, `PointsPage.js`, `tests/test_points_tracking.py`, ~20 legacy point-tracking helpers in `services/api.js`, the Points tab inside `MyFlightPage.js`, sidebar's "Point Tracking" item. `routes/parent.py` `/my-cadet/points` + `/admin-preview/{pid}/points` rewired to query `inspection_scores`. 18/18 backend tests pass in `test_inspections.py`. Spec-exact verified: Knowledge Day 2 Alpha 1 absent → 88.89%; Drill 51/54 = 94.44%; CTF avg 91.7%; CTF pts 183.33; CTS avg 45.8%; Day 3 Weekly Change Alpha = −183.3.
- **Org Chart In-Panel Reassign Picker + Mobile API Parity** (Feb 2026): New backend `PUT /api/org-chart/roles/{role_id}/assign-user` body `{user_id, propagate=true}` is the canonical "drop a user into a slot" endpoint. With `propagate=true` it uses the new `derive_user_updates_for_position` inverse mapper to apply role / squadron / flight / cadre_position / support_section changes to the target user, then the existing `auto_sync_org_chart` writes the chart row — so the users table and the chart never drift. With `propagate=false` (admins only) it writes `assigned_name` + `assigned_user_id` directly. Exec Cadre scoping matches `PUT /users/{id}/role` (cadre-bucket only, never PROTECTED_ROLES). Also added `PUT /api/org-chart/roles/{role_id}/clear-assignment` for blanking a slot without touching the user. Frontend `OrgChartPage.js` side-panel now renders a "Reassign user" button → expands into an inline autocomplete picker (live filter by name/email/CAPID) over `GET /users`, plus a red Clear button. Picker shows each user's role + CAPID + current squadron/flight; selecting one fires the assign endpoint and the panel re-renders with the new assigned name. `MOBILE_BACKEND_INTEGRATION.md` updated: Org Chart endpoint table grew from 7+1 to 10+1, new "Mobile parity notes" subsection documents template caching strategy, live-sync polling, branch palette hex values, and the canonical position-ID conventions (Flight IDs `<prefix>-flt-<letter>-cmdr|sgt`). Recent-Backend-Changes section #5 added covering the entire Phase 1+2 work. Org Chart Node data shape extended with `assigned_user_id`, `position_code`, `single_occupant`, `allowed_participant_types`, `job_description`.

## Backlog
- **P0**: Frontend taxonomy literals (`basic_student/advanced_student/exec_cadre`) — **DONE Feb 2026**. Only intentional defensive read-time aliases remain in `RosterPage.js`.
- **P0**: Add `is_exec_cadre` toggle on Cadre participant edit UI.
- **P1**: `Needs Review` filter chip + admin resolution flow on Roster page.
- **P2**: Senior Barracks individual room assignments (TR-106, TR-107, TR-105).
- **P2**: Code quality — 118 missing React hook deps warnings (non-blocking); refactor oversized `AdminPage.js`, `logistics.py`, `HealthServicesDashboard.js`, `routes/users.py` (916 lines) into smaller modules.
- **P2**: Schedule Sync (Excel auto-publish), Honor Agreements digital signature flow.

## Test Credentials
See `/app/memory/test_credentials.md`.
